{{ 'section-marquee.css' | asset_url | stylesheet_tag }}
{{ 'section-icon-marquee.css' | asset_url | stylesheet_tag }}


<div class="marquee icon-marquee">
  <div class="marquee__container">
    {% for block in section.blocks %}
      <div class="marquee__item">
        <img src="{{ block.settings.image | img_url: 'master' }}" />
      </div>
    {% endfor %}
  </div>
  <div class="marquee__container">
    {% for block in section.blocks %}
      <div class="marquee__item">
        <img src="{{ block.settings.image | img_url: 'master' }}" />
      </div>
    {% endfor %}
  </div>
</div>

{% schema %}
  {
    "name": "Icon Marquee",
    "settings": [],
    "blocks": [
      {
        "type": "marquee_item",
        "name": "Marquee Item",
        "settings": [
          {
            "type": "image_picker",
            "id": "image",
            "label": "Image"
          }
        ]
      }
    ],
    "presets": [
      {
        "name": "Icon Marquee"
      }
    ]
  }
{% endschema %}