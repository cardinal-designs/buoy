{{ 'component-supplements-drawer.css' | asset_url | stylesheet_tag }}

{% assign ss = section.settings %}

<div 
  id="supplementSideDrawer" 
  data-product-name="{{ ss.product_name }}" 
  class="supplement-side-drawer"
  {% comment %} {% if template == 'page.clear-pee-club' %}
    style="top: 0;"
  {% endif %} {% endcomment %}
>
  <div class="drawer__wrapper-main">

    <div class="drawer__header">
      <a href="javascript:void(0)" class="drawer__close js-close-supplement-drawer hide-mobile">
        <span>Close</span> {% render 'icon-close-drawer' %}</a>
    </div>  
    <div class="drawer__title-wrapper">
      {%- if ss.product_name != blank -%}
        <div class="drawer__product-name">{{ ss.product_name }}</div>
      {%- endif -%}
      {%- if ss.title != blank -%}
        <div class="drawer__title">{{ ss.title }}</div>
      {%- endif -%}
    </div>

    <div class="drawer__serving-container">
      {%- for block in section.blocks -%}
        {%- assign bs = block.settings -%}
          {%- if block.type == 'serving' -%}
            <div class="drawer__serving">
              <div class="drawer__serving-wrapper">
                {%- if bs.icon_serving != blank -%}
                  <img class="" src="{{ bs.icon_serving | img_url: 'master' }}"  alt="{{ bs.icon_serving.alt }}">
                {%- endif -%}
                {%- if bs.serving_title != blank -%}
                  <div class="drawer__serving-title">{{ bs.serving_title }}</div>
                {%- endif -%}
              </div>
              <ul class="drawer__serving-list">
                {%- if bs.serving_item_one != blank -%}
                  <li class="drawer__serving-item">{{ bs.serving_item_one }}</li>
                {%- endif -%}
                {%- if bs.serving_item_two != blank -%}
                  <li class="drawer__serving-item">{{ bs.serving_item_two }}</li>
                {%- endif -%}
                {%- if bs.serving_item_three != blank -%}
                  <li class="drawer__serving-item">{{ bs.serving_item_three }}</li>
                {%- endif -%}
              </ul>
            </div>
          {%- endif -%}
      {%- endfor -%}
    </div>

    <div class="drawer__recommended">
      {%- for block in section.blocks -%}
        {%- assign bs = block.settings -%}
          {%- if block.type == 'recommended' -%}
            {%- if bs.heading != blank -%}
              <div class="drawer__recommended-heading">{{ bs.heading }}</div>
            {%- endif -%}
            {%- if bs.title != blank -%}
              <div class="drawer__recommended-title">{{ bs.title }}</div>
            {%- endif -%}
            {%- if bs.sub_title != blank -%}
              <div class="drawer__recommended-subtitle">{{ bs.sub_title }}</div>
            {%- endif -%}
            {%- if bs.content != blank -%}
              <div class="drawer__recommended-content">{{ bs.content }}</div>
            {%- endif -%}
          {%- endif -%}
      {%- endfor -%}
    </div>

    <div class="drawer__mobile-continue hide-desktop">
      <a href="javascript:void(0)" class="drawer__continue-cta button button--full-width">Keep Shopping</a>        
    </div>

    <div class="drawer__table">
      {%- for block in section.blocks -%}
        {%- assign bs = block.settings -%}
        {%- if block.type == 'table' -%}
          {%- if bs.table_image != blank -%}
            <div class="drawer__table-image-wrapper">
              {% render 'lazy-image' image: bs.table_image, fade: true %}
            </div>
          {%- endif -%}
        {%- endif -%}
      {%- endfor -%}
    </div>

    <div class="drawer__ingredients">
      {%- for block in section.blocks -%}
        {%- assign bs = block.settings -%}
          {%- if block.type == 'ingredients' -%}
            {%- if bs.content != blank -%}
              <p class="drawer__ingredients-content">{{ bs.content }}</p>
            {%- endif -%}
          {%- endif -%}
      {%- endfor -%}
    </div>

    <div class="drawer__line"></div>

    <div class="drawer__info">
      {%- for block in section.blocks -%}
        {%- assign bs = block.settings -%}
          {%- if block.type == 'info' -%}
            <ul class="drawer__info-list">
              {%- if bs.icon_image != blank and bs.icon_info_one != blank -%}
                <li class="drawer__info-list-item">
                  <img class="" src="{{ bs.icon_image | img_url: 'master' }}"  alt="{{ bs.icon_image.alt }}">
                  <span>{{ bs.icon_info_one }}</span>
                </li>
              {%- endif -%}
              {%- if bs.icon_image_two != blank and bs.icon_info_two != blank -%}
                <li class="drawer__info-list-item">
                  <img class="" src="{{ bs.icon_image_two | img_url: 'master' }}"  alt="{{ bs.icon_image_two.alt }}">
                  <span>{{ bs.icon_info_two }}</span>
                </li>
              {%- endif -%}
              {%- if bs.icon_image_three != blank and bs.icon_info_three != blank -%}
                <li class="drawer__info-list-item">
                  <img class="" src="{{ bs.icon_image_three | img_url: 'master' }}"  alt="{{ bs.icon_image_three.alt }}">
                  <span>{{ bs.icon_info_three }}</span>
                </li>
              {%- endif -%}
              {%- if bs.icon_image_four != blank and bs.icon_info_four != blank -%}
                <li class="drawer__info-list-item">
                  <img class="" src="{{ bs.icon_image_four | img_url: 'master' }}"  alt="{{ bs.icon_image_four.alt }}">
                  <span>{{ bs.icon_info_four }}</span>
                </li>
              {%- endif -%}
              {%- if bs.icon_image_five != blank and bs.icon_info_five != blank -%}
                <li class="drawer__info-list-item">
                  <img class="" src="{{ bs.icon_image_five | img_url: 'master' }}"  alt="{{ bs.icon_image_five.alt }}">
                  <span>{{ bs.icon_info_five }}</span>
                </li>
              {%- endif -%}
            </ul>
          {%- endif -%}
      {%- endfor -%}
    </div>

    <div class="drawer__desktop-continue hide-mobile">
      <a href="javascript:void(0)" class="drawer__continue-cta button button--full-width">Keep Shopping</a>        
    </div>

  </div>
</div>


{% schema %}
  {
    "name": "Supplement Facts Drawer",
    "settings": [
      {
        "type": "text",
        "id": "product_name",
        "label": "Product Name"
      },
      {
        "type": "text",
        "id": "title",
        "label": "Title"
      }
    ],
    "blocks": [
      {
        "type": "serving",
        "name": "Serving",
        "settings": [
          {
            "type": "image_picker",
            "id": "icon_serving",
            "label": "Icon Serving"
          },
          {
            "type": "text",
            "id": "serving_title",
            "label": "Serving Title"
          },
          {
            "type": "text",
            "id": "serving_item_one",
            "label": "Serving Item One"
          },
          {
            "type": "text",
            "id": "serving_item_two",
            "label": "Serving Item Two"
          },
          {
            "type": "text",
            "id": "serving_item_three",
            "label": "Serving Item Three"
          }
        ]
      },
      {
        "type": "recommended",
        "name": "Recommended",
        "settings": [
          {
            "type": "text",
            "id": "heading",
            "label": "Heading"
          },
          {
            "type": "text",
            "id": "title",
            "label": "Title"
          },
          {
            "type": "text",
            "id": "sub_title",
            "label": "Subtitle"
          },
          {
            "type": "textarea",
            "id": "content",
            "label": "Content"
          }
        ]
      },
      {
        "type": "table",
        "name": "Table Image",
        "settings": [
          {
            "type": "image_picker",
            "id": "table_image",
            "label": "Table Image"
          }
        ]
      },
      {
        "type": "ingredients",
        "name": "Other Ingredients",
        "settings": [
          {
            "type": "textarea",
            "id": "content",
            "label": "Content"
          }
        ]
      },
      {
        "type": "info",
        "name": "More Info",
        "settings": [
          {
            "type": "image_picker",
            "id": "icon_image",
            "label": "Icon"
          },
          {
            "type": "text",
            "id": "icon_info_one",
            "label": "Icon Content"
          },
          {
            "type": "image_picker",
            "id": "icon_image_two",
            "label": "Icon Two"
          },
          {
            "type": "text",
            "id": "icon_info_two",
            "label": "Icon Content Two"
          },
          {
            "type": "image_picker",
            "id": "icon_image_three",
            "label": "Icon Three"
          },
          {
            "type": "text",
            "id": "icon_info_three",
            "label": "Icon Content Three"
          },
          {
            "type": "image_picker",
            "id": "icon_image_four",
            "label": "Icon Four"
          },
          {
            "type": "text",
            "id": "icon_info_four",
            "label": "Icon Content Four"
          },
          {
            "type": "image_picker",
            "id": "icon_image_five",
            "label": "Icon Five"
          },
          {
            "type": "text",
            "id": "icon_info_five",
            "label": "Icon Content Five"
          }
        ]
      }
    ],
    "presets": [
      {
        "name": "Supplement Facts Drawer"
      }
    ]
  }
{% endschema %}