{{ 'section-select-faq.css' | asset_url | stylesheet_tag }}


<div {% if section.settings.background != blank %}style="background: {{ section.settings.background }};"{% endif %} class="text-image-toggle__container">
 <div class="page-width">
   <div class="text-image-toggle__text">
     <h1>{{ section.settings.title }}</h1>
     <p>{{ section.settings.subtitle }}</p>
     {% for block in section.blocks %}
      <div class="text-image-toggle__button">
        <span>{{ block.settings.title }}</span>
      </div>
     {% endfor %} 
   </div>
   <div class="text-image-toggle__images">
     {% for block in section.blocks %}
      <img src="{{ block.settings.image | img_url: 'master' }}"/>
     {% endfor %} 
   </div>
 </div>
</div>

{% schema %}
{
  "name": "Select FAQ",
  "class": "spaced-section--full-width",
  "tag": "section",
  "settings": [
	{
      "type": "text",
      "id": "title",
      "label": "Title"
    },
    {
      "type": "text",
      "id": "subtitle",
      "label": "Subtitle"
    },
    {
      "type": "checkbox",
      "id": "supplements",
      "label": "Show Supplments Link?",
      "default": true
    },
    {
      "type": "color",
      "id": "background",
      "label": "Background" 
    }
  ],
  "blocks": [
    {
      "type": "block",
      "name": "Block",
      "settings": [
        {
          "type": "text",
          "id": "title",
          "label": "Title"
        },
        {
          "type": "image_picker",
          "id": "image",
          "label": "Image"
        }
      ]
    }
  ],
   "presets": [
      {
        "name": "Text Image Toggle"
      }
    ]
}
{% endschema %}